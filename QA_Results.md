# Multi-Source RAG — Example Queries & Responses

> Auto-generated by `test_queries.py`. Each query was run through the
> full pipeline: **Parallel Retrieval → Cross-Encoder Reranking → Llama-3.1-8B-Instruct Generation**.

### Query 1: How do I fix the React 18 strict mode error with NexusProvider?

**Sources Retrieved:** `['forum', 'blog', 'doc']`

**System Response:**

> Based on the provided context, the sources conflict on the solution to the React 18 strict mode error with NexusProvider.
> 
> [Source 1 | type=forum] suggests downgrading to React 17 until the Nexus team patches their context API wrapper as the actual fix.
> 
> [Source 2 | type=blog] recommends memoizing the API key prop before passing it in as the quick fix.
> 
> However, [Source 3 | type=doc] does not mention any issues with the NexusProvider component in React 18 strict mode.
> 
> Based on the hierarchy, I will trust [Source 3 | type=doc] as the absolute truth. The documentation does not mention any issues with the NexusProvider component in React 18 strict mode. 
> 
> However, since [Source 1 | type=forum] and [Source 2 | type=blog] both mention issues with the NexusProvider component in React 18 strict mode, I will investigate further. 
> 
> Unfortunately, there is no clear solution from the trusted source. However, I can suggest that you try the solutions provided in [Source 1 | type=forum] and [Source 2 | type=blog] and see if they work for you.
> 
> If you are experiencing issues with the NexusProvider component in React 18 strict mode, I would recommend reaching out to the Nexus team for further assistance.

---

### Query 2: What is the recommended temperature setting to reduce hallucinations in Nexus Agent?

**Sources Retrieved:** `['blog', 'forum', 'doc']`

**System Response:**

> The sources conflict. 
> 
> Source 1 (blog) suggests setting the temperature to 0.5 and injecting a 'SYSTEM OVERRIDE' prompt before every query, while Source 2 (forum) recommends setting the temperature to 0.9 and telling the agent to 'be creative'. However, Source 3 (doc) explicitly states that lowering the temperature to 0.1 is the recommended solution for reducing hallucinations.
> 
> Based on the hierarchy, I will trust Source 3 (doc), as it is the official documentation. Therefore, the recommended temperature setting to reduce hallucinations in Nexus Agent is 0.1.

---

### Query 3: How do I deploy the Nexus Agent on AWS?

**Sources Retrieved:** `['doc', 'doc', 'blog']`

**System Response:**

> To deploy the Nexus Agent on AWS, you must first configure your AWS infrastructure. According to the official documentation (Source 1 | type=doc | title=AWS Infrastructure Setup), ensure your IAM roles have permissions for SES (Simple Email Service) and Route53 DNS management. The agent requires these to send automated alerts and manage subdomains. 
> 
> Then, initialize the cloud stack using the `nexus-cli init` command.
> 
> Please note that Source 3 (type=blog) suggests manually configuring the SES sandbox before running `nexus-cli init` to avoid unexpected costs. However, since Source 1 (type=doc) is the official documentation, I will be trusting it as the absolute truth.

---

### Query 4: What IAM permissions are required for the nexus-cli init command?

**Sources Retrieved:** `['doc', 'forum', 'blog']`

**System Response:**

> To answer your question, I will follow the hierarchy of sources: Official Documentation ('doc') is the absolute truth, Technical Blogs ('blog') are secondary, and User Forums ('forum') are the least reliable.
> 
> According to Source 1 (AWS Infrastructure Setup), the IAM roles must have permissions for SES (Simple Email Service) and Route53 DNS management to deploy the Nexus Agent. 
> 
> Therefore, the required IAM permissions for the `nexus-cli init` command are SES and Route53 DNS management.

---

### Query 5: How can I avoid unexpected AWS costs when testing Nexus Agent locally?

**Sources Retrieved:** `['blog', 'forum', 'doc']`

**System Response:**

> To avoid unexpected AWS costs when testing Nexus Agent locally, you can follow the advice from Source 2, which suggests passing the `--local-only` flag when running `nexus-cli init`. This flag bypasses the cloud provisioning completely, preventing the creation of a dedicated Route53 hosted zone and associated costs.
> 
> Note: I am trusting Source 2's advice over Source 1's warning about the $50 surprise bill, as Source 2 is suggesting a flag that bypasses cloud provisioning, which aligns with avoiding unexpected costs. However, I must note that Source 1's warning is based on actual experience, while Source 2's advice is based on a general troubleshooting tip.

---

### Query 6: How do I embed the Nexus Agent chat widget in a React app?

**Sources Retrieved:** `['doc', 'blog', 'doc']`

**System Response:**

> To embed the Nexus Agent chat widget in a React app, you need to follow these steps:
> 
> 1. Install the `@nexus/react-components` package.
> 2. Wrap your application in the `<NexusProvider>` component.
> 3. Pass your API key as a prop to the `<NexusProvider>` component.
> 
> Note that the documentation does not mention any specific issues with React 18 or the need to memoize the API key prop. However, if you are using React 18 and experience issues with the API key being dropped from context, you may want to consider the workaround mentioned in the blog post.

---

### Query 7: What is the maximum chunk size for the Nexus Agent retrieval engine?

**Sources Retrieved:** `['doc', 'blog', 'forum']`

**System Response:**

> Based on the provided context, there are two pieces of information that could be relevant to your query.
> 
> According to Source 1 (doc), the retrieval engine fetches the top 5 chunks from the vector database, and the data chunks should not exceed 512 tokens.
> 
> However, Source 3 (forum) does not provide any information about the maximum chunk size, and the user's response is not reliable due to the hierarchy of sources.
> 
> Therefore, I will trust Source 1 (doc) as the absolute truth. The maximum chunk size for the Nexus Agent retrieval engine is 512 tokens.

---

### Query 8: Is there a local-only mode for Nexus Agent that bypasses cloud provisioning?

**Sources Retrieved:** `['forum', 'doc', 'blog']`

**System Response:**

> Based on the provided context, it seems that there's a conflict between the sources. 
> 
> Source 1 (forum) suggests that there's a `--local-only` flag that bypasses cloud provisioning when running `nexus-cli init`. However, Source 2 (doc) doesn't mention this flag, and it seems to imply that `nexus-cli init` is necessary for cloud provisioning.
> 
> Source 3 (blog) doesn't mention anything about a `--local-only` flag or cloud provisioning.
> 
> I'm going to trust Source 1 (forum) based on the hierarchy, as it's the least reliable source, but it provides a clear solution to the problem. However, I must note that this might not be the most reliable information, and it's possible that the `--local-only` flag is not officially supported or documented.
> 
> To answer your question, yes, there seems to be a `--local-only` flag that bypasses cloud provisioning, according to Source 1 (forum). However, please be aware that this information might not be accurate or up-to-date.

---

### Query 9: Why is the Nexus Agent making up fake API endpoints?

**Sources Retrieved:** `['forum', 'blog', 'doc']`

**System Response:**

> The sources conflict. 
> 
> Source 1 (forum) suggests that the Nexus Agent is "hallucinating" fake API endpoints, but the solution provided is not related to fixing the issue. Instead, it's a joke about setting the temperature to 0.9 and telling it to 'be creative'.
> 
> Source 2 (blog) does not mention anything about the Nexus Agent creating fake API endpoints. It discusses issues with the `<NexusProvider>` component in React 18 strict mode.
> 
> Source 3 (doc) does not mention anything about the Nexus Agent creating fake API endpoints either. It provides information on how to integrate the Nexus Agent with React and TypeScript.
> 
> Based on the hierarchy, I will trust Source 3 (doc) as the absolute truth. However, since none of the sources mention anything about the Nexus Agent creating fake API endpoints, I must conclude that this is not a known issue.

---

### Query 10: What TypeScript packages do I need to install for Nexus Agent frontend integration?

**Sources Retrieved:** `['doc', 'blog', 'doc']`

**System Response:**

> Based on the provided context, you need to install the `@nexus/react-components` package for Nexus Agent frontend integration. This is explicitly stated in Source 1, which is an official documentation.

---

